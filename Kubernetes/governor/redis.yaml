apiVersion: v1
kind: Service
metadata:
  name: governor-redis
spec:
  selector:
    app: governor-redis
  ports:
    - protocol: TCP
      port: 6349
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: governor-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: governor-redis
  template:
    metadata:
      labels:
        app: governor-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          args: ["redis-server", "--requirepass", "password"]
          ports:
            - containerPort: 6379
