apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zk-governor-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zk-algonexus-datalog-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: governor-zookeeper
spec:
  selector:
    app: governor-zookeeper
  ports:
    - port: 2181
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: governor-zookeeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: governor-zookeeper
  template:
    metadata:
      labels:
        app: governor-zookeeper
    spec:
      containers:
        - name: governor-zookeeper
          image: confluentinc/cp-zookeeper:7.4.0
          ports:
            - containerPort: 2181
          env:
            - name: ZOOKEEPER_CLIENT_PORT
              value: "2181"
            - name: ZOOKEEPER_TICK_TIME
              value: "2000"
          volumeMounts:
            - name: zk-algonexus-data
              mountPath: /var/lib/zookeeper/data
            - name: zk-algonexus-datalog
              mountPath: /var/lib/zookeeper/log
          readinessProbe:
            tcpSocket:
              port: 2181
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
      volumes:
        - name: zk-algonexus-data
          persistentVolumeClaim:
            claimName: zk-governor-data-pvc
        - name: zk-algonexus-datalog
          persistentVolumeClaim:
            claimName: zk-algonexus-datalog-pvc
