apiVersion: v1
kind: Service
metadata:
  name: forge-redis
spec:
  selector:
    app: forge-redis
  ports:
    - protocol: TCP
      port: 6319
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forge-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: forge-redis
  template:
    metadata:
      labels:
        app: forge-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          args: ["redis-server", "--requirepass", "password"]
          ports:
            - containerPort: 6379
