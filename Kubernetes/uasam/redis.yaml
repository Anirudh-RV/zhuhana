apiVersion: v1
kind: Service
metadata:
  name: uasam-redis
spec:
  selector:
    app: uasam-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: uasam-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uasam-redis
  template:
    metadata:
      labels:
        app: uasam-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          args: ["redis-server", "--requirepass", "password"]
          ports:
            - containerPort: 6379
