services:
  cortex-service:
    build:
      context: ../
      dockerfile: dockerfiles/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=llama3:8b-instruct-q4_0
