# Load-test result 20260529-155850

| metric | value |
|---|---|
| target | `http://algonexus:8080/v1/ordermanager/submit` |
| script / policy / logs | `submit_constant.js` / `failfast` / `off` |
| target rate | 4200/s |
| **accepted (HTTP)** | 126001 reqs @ 3779.902498974836/s |
| HTTP failed | 0.0% |
| ingest latency med / p90 / p95 | 0.14ms / 0.23ms / 0.85ms |
| dropped iterations (k6 VU cap) | None |
| **redis stream / group** | `orderstream:strategy-1` / `group:order:strategy-1` |
| **XINFO GROUPS -lag** | 0 |
| XINFO GROUPS -pending | 0 |
| **XINFO GROUPS -entries-read** | 126001 |
| XLEN (stream length) | 126001 |
| memory idle -> after | 242.1MiB -> 308.6MiB (cpu 0.06%) |
| pipeline errors | 0 |
| **VERDICT: end-to-end zero loss** | YES |

```
$ redis-cli XINFO GROUPS orderstream:strategy-1
name
group:order:strategy-1
consumers
1
pending
0
last-delivered-id
1780095637536-2
entries-read
126001
lag
0

$ redis-cli XLEN orderstream:strategy-1
126001
```

Ground truth = redis XINFO (lag/pending/entries-read) + XLEN, not sampled log counts.
Zero-loss criterion: lag==0 AND pending==0 AND entries-read>=accepted AND errors==0.
