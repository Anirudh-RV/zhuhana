services:
  node-app:
    build:
      context: "../"
      dockerfile: Dockerfile
    command: npm run start
    env_file:
      - "../env/local.env"
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
