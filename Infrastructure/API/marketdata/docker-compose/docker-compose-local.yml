version: "3.9"

services:
  api-marketdata:
    build: ..
    container_name: api-marketdata-container
    ports:
      - "8000:8080"
    environment:
      - PORT=8080
    volumes:
      - .:/app
    restart: unless-stopped
